<%= form_with(model: message, remote: true) do |form| %>

<div class="row">
    <%= form.hidden_field :chatroom_id %>
  <div class="col-sm-10">
    <%= form.text_area :body, style: "width:100%" %>
  </div>
  <div class="col-sm-2">
    <%= form.submit %>
  </div>
</div>
<% end %>
